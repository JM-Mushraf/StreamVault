namespace SV.Common.Middleware
{
    // Placeholder for library project to avoid ASP.NET Core dependency in common library.
    // Actual middleware with DB error logging is implemented in the main Web project.
    public class ExceptionMiddleware
    {
        // Intentionally left minimal to avoid cross-project dependencies.
    }
}
